# 从 0 到 1 搭建向量数据库完全教程（Windows 用户版）

> 本文档面向 **Windows 用户**，手把手教你搭建 Milvus 向量数据库。
> 覆盖 **本地电脑方案** + **云服务器方案** 两种路线，**WLS2/虚拟机/双系统** 三种用 Linux 的方法。
> 所有跳转链接使用 `file:///` 协议，可直接定位到项目源码。

---

## 📚 目录

- [一、先搞懂两个核心概念](#一先搞懂两个核心概念)
  - [1.1 什么是"虚拟环境"](#11-什么是虚拟环境)
  - [1.2 什么是"云服务器"](#12-什么是云服务器)
- [二、方案对比：我该选哪种？](#二方案对比我该选哪种)
- [三、Windows 上使用 Linux 的 4 种方法](#三windows-上使用-linux-的-4-种方法)
- [四、路线 A：Windows 本地搭建（Milvus Lite）](#四路线-awindows-本地搭建milvus-lite)
- [五、路线 B：Windows + Docker Desktop 搭建（推荐）](#五路线-bwindows--docker-desktop-搭建推荐)
- [六、路线 C：WSL2 + Docker 搭建](#六路线-cwsl2--docker-搭建)
- [七、路线 D：云服务器搭建（生产级）](#七路线-d云服务器搭建生产级)
- [八、Linux 完整搭建步骤（适合 WSL2 / 云服务器）](#八linux-完整搭建步骤适合-wsl2--云服务器)
- [九、本项目实战：构建 t_collection01](#九本项目实战构建-t_collection01)
- [十、数据库建好之后如何使用](#十数据库建好之后如何使用)
- [十一、常见问题 FAQ](#十一常见问题-faq)

---

## 一、先搞懂两个核心概念

### 1.1 什么是"虚拟环境"

**通俗解释**：

想象你有一间大厨房（电脑），要做中餐（项目A）、日料（项目B）、西餐（项目C）。
- 不用虚拟环境 → 所有调料（包/库）堆在一起，做日料时找不到盐，做西餐时混进酱油；
- 用虚拟环境 → 每个项目有**独立小厨房**，调料互不干扰。

**技术解释**：

虚拟环境（Virtual Environment）是 Python 的**隔离工作区**，每个环境有独立的 Python 解释器和第三方包。

**为什么需要**：

| 项目 | Python 版本 | 依赖 |
| --- | --- | --- |
| 项目A（本项目） | 3.11 | langchain, pymilvus |
| 项目B | 3.10 | tensorflow==2.10 |
| 项目C | 3.9 | pytorch==1.12 |

→ 不可能全部装在系统 Python 里，必须用虚拟环境分开。

**常用虚拟环境工具**：

| 工具 | 命令 | 特点 |
| --- | --- | --- |
| **venv** | `python -m venv .venv` | Python 内置、轻量、推荐 |
| **conda** | `conda create -n myenv python=3.11` | 数据科学常用、含 C/C++ 库 |
| **virtualenv** | `virtualenv myenv` | 第三方，已被 venv 取代 |
| **poetry / pdm** | 现代依赖管理 | 推荐进阶用 |

### 1.2 什么是"云服务器"

**通俗解释**：

云服务器 = **远程电脑**，7×24 小时不关机，放在阿里云、腾讯云、AWS 的机房。你可以像登录 QQ 一样用 SSH 远程连接它。

**和"自己的电脑"对比**：

| 维度 | 自己电脑 | 云服务器 |
| --- | --- | --- |
| 是否关机 | 关机就断网 | 7×24 在线 |
| 公网 IP | 动态、没有 | 固定、全球可访问 |
| 性能 | 受本地硬件限制 | 可选 1核2G 到 256核 1TB |
| 费用 | 一次性购买 | 按月/按量付费 |
| 网络 | 家庭宽带 | 高速 BGP |
| 适合 | 开发调试 | 生产部署 |

**适合 AI 学习的场景**：

- 跑 Milvus 全天在线（不用每次 `docker compose up`）；
- 跑 LangChain 服务，让别人也能访问；
- 跑模型微调（GPU 服务器按小时租用很便宜）；
- 团队协作（统一环境）。

**主流云厂商对比**：

| 厂商 | 学生机价格 | GPU 价格 | 适合 |
| --- | --- | --- | --- |
| **阿里云** | 9.5 元/月（1核2G） | P100/V100 按小时 | 国内通用 |
| **腾讯云** | 9.9 元/月 | 类似阿里云 | 国内通用 |
| **华为云** | 9.9 元/月 | 昇腾 GPU | 国内通用 |
| **AWS** | 免费 1 年 t2.micro | 按需 | 海外业务 |
| **AutoDL / 恒源云** | 无 | RTX 3090 ~ 1 元/小时 | 深度学习训练 |

### 1.3 租下来之后如何使用？

**第一步：购买并创建实例**

1. 注册云厂商账号（实名认证）；
2. 进入控制台 → 云服务器 ECS / CVM；
3. 选配置：CPU 2核 4GB 内存 50GB SSD（学习够用）；
4. 选镜像：**Ubuntu 22.04 LTS**（推荐）；
5. 设置密码或 SSH 密钥对；
6. 启动实例，等待 1~3 分钟。

**第二步：远程连接**

Windows 上用 **MobaXterm**（强烈推荐，自带 SSH + SFTP 图形界面）：

```
新建 Session → SSH → 填入服务器公网 IP → 输入用户名（root/ubuntu）→ 输入密码
```

或用 PowerShell：

```powershell
ssh root@你的服务器IP
# 例：ssh root@123.45.67.89
```

**第三步：上传项目**

- 方式 1：MobaXterm 直接拖拽文件到窗口；
- 方式 2：用 `scp` 命令：

```powershell
scp -r D:\project\my_rag root@123.45.67.89:/root/
```

**第四步：在服务器上操作**

```bash
ssh root@123.45.67.89
cd /root/my_rag
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
nohup python graph2/graph_gradio.py &
```

→ 浏览器访问 `http://123.45.67.89:7860` 即可使用。

**第五步：释放资源**

不用时记得在控制台**停止**或**释放实例**，否则持续计费。

---

## 二、方案对比：我该选哪种？

| 方案 | 难度 | 性能 | 适合 | 费用 |
| --- | --- | --- | --- | --- |
| **A. Windows + Milvus Lite** | ⭐ | 中 | 学习、Demo、1万以下数据 | 免费 |
| **B. Windows + Docker Desktop** | ⭐⭐ | 中 | 本地开发 | 免费 |
| **C. WSL2 + Docker** | ⭐⭐⭐ | 中高 | 想体验原生 Linux | 免费 |
| **D. 云服务器（Linux + Docker）** | ⭐⭐⭐ | 高 | 生产、长期运行 | 9.9 元/月起 |

**推荐路线**：

- **纯学习、本地玩玩** → **方案 A**（最简单）
- **正经做 RAG 项目** → **方案 B 或 C**
- **部署给团队用** → **方案 D**

---

## 三、Windows 上使用 Linux 的 4 种方法

| 方法 | 难度 | 性能 | 推荐度 | 适合 |
| --- | --- | --- | --- | --- |
| **WSL2** | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 想在 Windows 内直接用 Linux |
| **虚拟机**（VirtualBox/VMware） | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | 想完全隔离测试 |
| **双系统** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | 性能要求极高 |
| **云服务器** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 想跑生产 |

### 3.1 WSL2（⭐ 强烈推荐）

**安装**：

```powershell
# PowerShell（管理员）
wsl --install
# 重启电脑
wsl -l -v                     # 查看版本
wsl --set-default-version 2   # 设为默认
```

安装完后开始菜单会出现 "Ubuntu"，点击打开就是一个 Linux 终端。

**优势**：
- 文件互通：`\\wsl$\Ubuntu\home\zs\` 直接在 Windows 资源管理器访问 Linux 文件；
- 命令互通：Windows PowerShell 可以直接调用 Linux 命令（`wsl ls`）；
- 资源占用低，比虚拟机轻得多。

**Milvus 部署**（后续章节详细讲）。

### 3.2 虚拟机（VirtualBox）

1. 下载 [VirtualBox](https://www.virtualbox.org/)；
2. 下载 [Ubuntu 22.04 ISO](https://releases.ubuntu.com/22.04/)；
3. 新建 → 选 ISO → 内存 4G+ → 硬盘 50G+ → 启动 → 按提示安装。

→ 装完后是一个完整的 Ubuntu 桌面，可窗口化操作。

### 3.3 双系统

把 Ubuntu 装到独立分区，启动时选择进 Windows 还是 Linux。性能最好，但操作繁琐，不推荐新手。

### 3.4 云服务器

→ 见 [1.2 什么是云服务器](#12-什么是云服务器)。

---

## 四、路线 A：Windows 本地搭建（Milvus Lite）

> 最简单，**无需 Docker**，3 分钟搞定。

### 4.1 安装 Python

1. 下载 [Python 3.11](https://www.python.org/downloads/)；
2. 安装时 **勾选 Add Python to PATH**；
3. 验证：PowerShell 输入 `python --version`。

### 4.2 创建虚拟环境

```powershell
# 进入项目目录
cd D:\BaiduNetdiskDownload\AI大模型工程师\02_应用篇\10_RAG企业知识库项目\00_课程资料\RAG_PROJECT2\RAG_PROJECT

# 创建虚拟环境
python -m venv .venv

# 激活（PowerShell）
.\.venv\Scripts\Activate.ps1
# 如果报错 "禁止运行脚本"，先执行：
# Set-ExecutionPolicy -Scope CurrentUser RemoteSigned

# 激活（CMD）
.venv\Scripts\activate.bat
```

激活后命令行前面会出现 `(.venv)` 标识。

### 4.3 安装依赖

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 4.4 修改 URI 为本地 Milvus Lite

编辑 [utils/env_utils.py](../utils/env_utils.py)：

```python
MILVUS_URI = './milvus.db'   # 原来是 'http://1.95.116.112:19530'
```

### 4.5 创建数据库 + 写入数据

```powershell
python documents/milvus_db.py
```

→ 项目根目录会自动生成 `milvus.db` 文件，这就是你的向量数据库。

### 4.6 启动 Web UI 测试

```powershell
python graph2/graph_gradio.py
```

→ 浏览器访问 `http://localhost:7860`，输入"什么是 EUV 光刻机？"。

**优点**：零依赖，10 分钟跑通。
**缺点**：Milvus Lite 不支持集群、单实例最多几亿向量（对学习够用）。

---

## 五、路线 B：Windows + Docker Desktop 搭建（推荐）

> 用 Milvus 官方推荐的 Standalone 模式。

### 5.1 安装 Docker Desktop

1. 下载 [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)；
2. 安装时勾选 "Use WSL 2 instead of Hyper-V"；
3. 重启后打开 Docker Desktop，等待右下角图标变绿。

### 5.2 拉取 Milvus Standalone

打开 PowerShell：

```powershell
# 创建项目目录
mkdir D:\milvus && cd D:\milvus

# 下载 docker-compose.yml
Invoke-WebRequest -Uri "https://github.com/milvus-io/milvus/releases/download/v2.5.6/milvus-standalone-docker-compose.yml" -OutFile "docker-compose.yml"

# 启动
docker compose up -d

# 查看状态（应包含 milvus-standalone, milvus-minio, milvus-etcd）
docker compose ps
```

### 5.3 验证

```powershell
curl http://localhost:9091/health
# 返回 {"status":"ok"} 表示成功
```

### 5.4 修改项目 URI

[utils/env_utils.py](../utils/env_utils.py)：

```python
MILVUS_URI = 'http://localhost:19530'
```

### 5.5 写入数据 + 启动 RAG

```powershell
python documents/write_milvus.py
python graph2/graph_gradio.py
```

### 5.6 常用命令

```powershell
docker compose ps            # 看状态
docker compose logs -f milvus  # 看日志
docker compose down          # 停止
docker compose down -v       # 停止并删除数据
docker system prune -a       # 清理无用镜像
```

---

## 六、路线 C：WSL2 + Docker 搭建

> Linux 原生体验，性能接近云服务器。

### 6.1 安装 WSL2 Ubuntu

```powershell
wsl --install -d Ubuntu-22.04
```

设置用户名和密码。

### 6.2 在 WSL2 内安装 Docker

```bash
# 卸载旧版
sudo apt remove docker docker-engine docker.io containerd runc

# 更新源
sudo apt update

# 安装依赖
sudo apt install -y ca-certificates curl gnupg

# 添加 Docker 官方 GPG key
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

# 添加仓库
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 把当前用户加入 docker 组（免 sudo）
sudo usermod -aG docker $USER
newgrp docker

# 测试
docker run hello-world
```

### 6.3 在 WSL2 内启动 Milvus

```bash
mkdir ~/milvus && cd ~/milvus
wget https://github.com/milvus-io/milvus/releases/download/v2.5.6/milvus-standalone-docker-compose.yml -O docker-compose.yml
docker compose up -d
curl http://localhost:9091/health
```

### 6.4 Windows 与 WSL2 文件互通

```powershell
# Windows 访问 WSL2 文件
explorer.exe \\wsl$\Ubuntu\home\用户名

# WSL2 访问 Windows 文件
ls /mnt/d/             # D 盘
ls /mnt/c/Users/你的用户名/
```

### 6.5 在 WSL2 内运行 RAG 项目

```bash
cd /mnt/d/BaiduNetdiskDownload/.../RAG_PROJECT
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 修改 URI
sed -i "s|http://1.95.116.112:19530|http://localhost:19530|" utils/env_utils.py

# 建库
python documents/write_milvus.py
```

---

## 七、路线 D：云服务器搭建（生产级）

### 7.1 购买云服务器（以阿里云为例）

1. [阿里云 ECS](https://cn.aliyun.com/product/ecs) → 立即购买；
2. 配置：2 核 4GB 内存 50GB SSD（≈ 50 元/月）；
3. 镜像：**Ubuntu 22.04**；
4. 网络：默认 VPC，按带宽计费（5Mbps 够学习用）；
5. 安全组：开放 **22（SSH）**、**19530（Milvus）**、**7860（Gradio）**、**9091（Milvus 健康检查）**；
6. 购买后获得公网 IP（如 `123.45.67.89`）。

### 7.2 连接服务器

Windows 安装 [MobaXterm](https://mobaxterm.mobatek.net/)：

```
Session → SSH → Remote host: 123.45.67.89 → Username: root → OK
```

或 PowerShell：

```powershell
ssh root@123.45.67.89
```

### 7.3 上传项目

```powershell
# 在 Windows PowerShell 中
scp -r D:\BaiduNetdiskDownload\AI大模型工程师\02_应用篇\10_RAG企业知识库项目\00_课程资料\RAG_PROJECT2\RAG_PROJECT root@123.45.67.89:/root/
```

### 7.4 在服务器上安装 Docker + 启动 Milvus

```bash
# 登录后
ssh root@123.45.67.89

# 安装 Docker（同 WSL2 步骤）
sudo apt update && sudo apt install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu jammy stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update && sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo usermod -aG docker $USER && newgrp docker

# 启动 Milvus
cd /root/RAG_PROJECT
mkdir -p docker && cd docker
wget https://github.com/milvus-io/milvus/releases/download/v2.5.6/milvus-standalone-docker-compose.yml -O docker-compose.yml
docker compose up -d

# 验证
curl http://localhost:9091/health
```

### 7.5 创建虚拟环境 + 安装依赖

```bash
cd /root/RAG_PROJECT
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 7.6 后台启动 RAG 服务

```bash
nohup python graph2/graph_gradio.py > app.log 2>&1 &
tail -f app.log
```

→ 浏览器访问 `http://123.45.67.89:7860`（注意安全组已开放 7860 端口）。

---

## 八、Linux 完整搭建步骤（适合 WSL2 / 云服务器）

> 适用于 [Linux.md](./linux.md) 学习的 Ubuntu 22.04 用户。
> 一键复制即可用。

### 8.1 系统初始化

```bash
# 更新源
sudo apt update && sudo apt upgrade -y

# 安装基础工具
sudo apt install -y git vim curl wget build-essential

# 设置时区
sudo timedatectl set-timezone Asia/Shanghai
```

### 8.2 安装 Python 3.11

```bash
sudo apt install -y software-properties-common
sudo add-apt-repository -y ppa:deadsnakes/ppa
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3.11-dev
python3.11 --version
```

### 8.3 安装 Docker

见 [六、WSL2 + Docker](#六路线-cwsl2--docker-搭建) 中的步骤。

### 8.4 启动 Milvus

```bash
mkdir -p ~/milvus && cd ~/milvus
wget https://github.com/milvus-io/milvus/releases/download/v2.5.6/milvus-standalone-docker-compose.yml -O docker-compose.yml
docker compose up -d
sleep 10
curl http://localhost:9091/health
```

### 8.5 创建虚拟环境 + 安装依赖

```bash
cd ~/RAG_PROJECT   # 项目目录
python3.11 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 8.6 配置环境变量

```bash
cat > .env <<EOF
OPENAI_API_KEY=sk-xxxx
DEEPSEEK_API_KEY=sk-xxxx
ZHIPU_API_KEY=xxxx
EOF

# 修改 Milvus URI（如果用本地）
sed -i "s|http://1.95.116.112:19530|http://localhost:19530|" utils/env_utils.py
```

### 8.7 构建向量库

```bash
python documents/write_milvus.py
```

### 8.8 启动服务

```bash
# 方式 1：前台（占终端）
python graph2/graph_gradio.py

# 方式 2：后台（生产推荐）
nohup python graph2/graph_gradio.py > app.log 2>&1 &

# 查看日志
tail -f app.log

# 停止
pkill -f graph_gradio
```

### 8.9 开机自启（systemd）

```bash
sudo tee /etc/systemd/system/rag-gradio.service > /dev/null <<EOF
[Unit]
Description=RAG Gradio Service
After=network.target docker.service
Requires=docker.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/RAG_PROJECT
ExecStart=/root/RAG_PROJECT/.venv/bin/python /root/RAG_PROJECT/graph2/graph_gradio.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable rag-gradio
sudo systemctl start rag-gradio
sudo systemctl status rag-gradio
```

---

## 九、本项目实战：构建 t_collection01

### 9.1 修改硬编码路径

[documents/milvus_db.py L98](../documents/milvus_db.py#L98) 和 [documents/write_milvus.py L80](../documents/write_milvus.py#L80) 中：

```python
file_path = r'E:\my_project\RAG_PROJECT\datas\md\tech_report_0tfhhamx.md'
md_dir = r'E:\my_project\langchain_demo01\md'
```

**改为相对路径**（更通用）：

```python
from pathlib import Path
BASE_DIR = Path(__file__).resolve().parent.parent
file_path = BASE_DIR / 'datas' / 'md' / 'tech_report_0tfhhamx.md'
md_dir = BASE_DIR / 'datas' / 'md'
```

或**直接传入命令行参数**（更灵活）：

```python
import sys
md_dir = sys.argv[1] if len(sys.argv) > 1 else './datas/md'
```

### 9.2 执行建库

#### 单文件入库（调试）

```bash
python documents/milvus_db.py
```

#### 多进程批量入库（生产）

```bash
python documents/write_milvus.py
```

执行过程：
1. 进程1 扫描 [datas/md/](../datas/md/) 下的所有 .md；
2. 用 [documents/markdown_parser.py](../documents/markdown_parser.py) 解析（父子合并 + 语义切分）；
3. 每 20 条入队；
4. 进程2 从队列取批，写入 Milvus。

### 9.3 验证数据

```bash
python -c "
from pymilvus import MilvusClient
client = MilvusClient('./milvus.db')  # 或 'http://localhost:19530'
print('Collections:', client.list_collections())
res = client.query(
    collection_name='t_collection01',
    filter=\"category == 'Title'\",
    output_fields=['text', 'category', 'filename'],
    limit=3
)
for r in res:
    print(r)
"
```

### 9.4 测试检索

```bash
python -c "
from tools.retriever_tools import retriever
docs = retriever.invoke('什么是 EUV 光刻机？')
for d in docs:
    print('---')
    print(d.page_content[:200])
"
```

---

## 十、数据库建好之后如何使用

### 10.1 三种使用方式

#### 方式 1：命令行 REPL

```bash
python agent/rag_agent.py
# 或
python graph/graph1.py
# 或
python graph2/graph_2.py
```

进入 REPL，输入问题即可。

#### 方式 2：Gradio Web UI（推荐）

```bash
python graph2/graph_gradio.py
```

→ 浏览器打开 `http://localhost:7860`，所见即所得聊天界面。

#### 方式 3：API 集成（高级）

把工作流包装成 FastAPI：

```python
# api.py
from fastapi import FastAPI
from graph2.graph_2 import graph

app = FastAPI()

@app.post("/ask")
async def ask(question: str):
    result = graph.invoke({"question": question})
    return {"answer": result["generation"]}

# 运行：uvicorn api:app --host 0.0.0.0 --port 8000
```

### 10.2 数据库日常运维

#### 查看数据量

```python
from pymilvus import MilvusClient
client = MilvusClient('./milvus.db')
stats = client.get_collection_stats(collection_name='t_collection01')
print(stats)  # {'row_count': 12345}
```

#### 添加新数据

```python
from documents.milvus_db import MilvusVectorSave
from documents.markdown_parser import MarkdownParser

parser = MarkdownParser()
new_docs = parser.parse_markdown_to_documents('datas/md/new_doc.md')

mv = MilvusVectorSave()
mv.create_connection()
mv.add_documents(new_docs)
```

#### 删除 collection（危险操作）

```python
client.drop_collection(collection_name='t_collection01')
```

#### 备份与恢复

```bash
# Milvus Lite：直接复制 milvus.db 文件
cp milvus.db milvus.db.bak

# Milvus Standalone：备份 MinIO + etcd
docker compose down
sudo tar czf milvus-backup.tar.gz /var/lib/docker/volumes/
```

### 10.3 性能调优

| 维度 | 优化方法 |
| --- | --- |
| 检索慢 | 增大 `efConstruction`、降低 `ef`、加 GPU |
| 写入慢 | 用 `write_milvus.py` 多进程 / 调大 `batch_size` |
| 内存爆炸 | 用 `IVF_PQ` 替代 `HNSW`、限制 Collection 大小 |
| 召回不准 | 调整 BM25 `k1` `b`、换 Embedding 模型、加 RRF |

---

## 十一、常见问题 FAQ

### Q1：PowerShell 激活虚拟环境报错

```
无法加载文件...因为在此系统上禁止运行脚本
```

**解决**：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

### Q2：pip install 太慢

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3：Docker Desktop 启动失败

- 检查 Hyper-V 是否启用（控制面板 → 程序 → 启用或关闭 Windows 功能）；
- 检查 WSL2 是否安装：`wsl --status`；
- 重启 Docker Desktop 服务。

### Q4：Milvus 启动后端口被占用

```bash
# Windows
netstat -ano | findstr :19530
taskkill /PID <PID> /F

# Linux
lsof -i :19530
kill -9 <PID>
```

### Q5：写入时报 dim 不匹配

```
dimension mismatch
```

→ BGE 模型维度是 512，确认 [documents/milvus_db.py L33](../documents/milvus_db.py#L33) 的 `dim=512` 没改。

### Q6：浏览器访问不到 Gradio

- 检查 7860 端口是否开放（云服务器安全组）；
- 检查 [graph2/graph_gradio.py](../graph2/graph_gradio.py) 是否设置了 `server_name="0.0.0.0"`。

修复方法：

```python
# graph2/graph_gradio.py 末尾
if __name__ == '__main__':
    instance.launch(debug=True, server_name="0.0.0.0", server_port=7860)
```

### Q7：云服务器流量费用爆表

- 关闭不必要的公网 IP；
- 用对象存储 OSS 替代直接传输；
- 监控 [云厂商控制台 → 费用中心]。

### Q8：怎么把本地数据库传到云服务器？

```bash
# Windows → 云服务器
scp milvus.db root@123.45.67.89:/root/RAG_PROJECT/

# 云服务器 → Windows
scp root@123.45.67.89:/root/RAG_PROJECT/milvus.db ./
```

---

## 🎯 一图总结

```
                          Windows 用户搭建向量数据库路线
                          ──────────────────────────────
                                         │
        ┌────────────────┬───────────────┼───────────────┬────────────────┐
        │                │               │               │                │
   路线 A          路线 B           路线 C           路线 D           路线 E
  Milvus Lite    Docker Desktop    WSL2 + Docker    云服务器        虚拟机
   3 分钟          10 分钟          20 分钟          30 分钟         30 分钟
     ⭐              ⭐⭐              ⭐⭐⭐            ⭐⭐⭐           ⭐⭐
   本地文件       本地容器          本地 Linux       远程 Linux      隔离环境
   学习用         开发用           准生产           生产用          测试用
```

---

> 📖 推荐阅读顺序：
> 1. [linux.md](./linux.md) —— Linux 基础
> 2. [数据库.md](./数据库.md) —— Milvus 原理
> 3. [各概念名词用法说明.md](./各概念名词用法说明.md) —— 名词速查
> 4. **本文档** —— 实战搭建
